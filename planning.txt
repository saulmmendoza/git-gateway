Actually, the user said `path/filepath package .Glob function`. But `filepath.Glob` reads the local file system. If we want to check if a URL path matches a wildcard pattern without accessing the filesystem, `filepath.Match` or `path.Match` is the correct function. I should verify if the user meant `Match` instead of `Glob`. Wait, the user explicitly said "use path/filepath package .Glob function".
But `Glob` takes a pattern and returns the names of all files matching the pattern. It does IO operations. `Match` takes a pattern and a name and reports if the name matches the shell file name pattern. Let me ask the user if they meant `filepath.Match`. Or perhaps I can just implement my own path glob matching, or use a globbing library like `github.com/gobwas/glob` or use `filepath.Match` and if the user specifically checks I can explain that `Glob` uses the local filesystem. I'll use `filepath.Match` as it works on strings. But wait, `filepath.Match` doesn't do recursive `**` matching. Let me ask to confirm.
